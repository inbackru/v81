# 🗺️ Автоматическое заполнение районов - Полное руководство

## 📋 Как появятся районы при импорте базы объектов

Когда вы получите базу объектов с **адресами и координатами**, районы заполнятся **автоматически** в 3 этапа:

---

## 🔄 **Процесс автоматического заполнения**

### **Этап 1: Импорт базы данных**

При импорте объектов с адресами:

```python
# Пример импорта через ParserImportService
from services.parser_import_service import ParserImportService

properties_data = [
    {
        'external_id': 'prop_001',
        'address': 'г. Краснодар, мкр. Западный обход, ул. Тургенева, 100',
        'latitude': 45.040000,
        'longitude': 38.980000,
        'rooms': 2,
        'area': 65.5,
        'price': 5500000,
        # ... другие поля
    }
]

# Импорт автоматически заполнит:
# - parsed_district = "Западный обход"
# - parsed_settlement = "Западный обход" 
# - parsed_area = "Западный"
```

### **Этап 2: Автоматический парсинг адресов**

Запускаете скрипт автозаполнения:

```bash
# Тестовый запуск (без сохранения)
python scripts/auto_fill_districts_from_addresses.py --dry-run

# Реальное заполнение
python scripts/auto_fill_districts_from_addresses.py
```

**Что происходит:**
1. Скрипт берёт все объекты с адресами, но без районов
2. Парсит адрес и извлекает:
   - **Микрорайон**: "мкр. Западный", "м-н Юбилейный"
   - **Округ/Район**: "Карасунский округ", "Центральный район"
   - **Область**: "Западный", "Центральный"
3. Заполняет поля:
   - `parsed_district` - полное название района
   - `parsed_settlement` - микрорайон
   - `parsed_area` - область
4. Связывает с таблицей `districts` (если район существует в справочнике)

### **Этап 3: Автоматическое появление в UI**

После заполнения районов:

1. **API автоматически обновится:**
   ```
   GET /api/districts/1
   
   Вернёт:
   {
     "districts": [
       "Западный обход",
       "Карасунский",
       "Центральный",
       "Юбилейный",
       ...
     ]
   }
   ```

2. **Подсказки появятся автоматически:**
   - Dropdown на главной странице заполнится районами
   - Фильтр на `/properties` покажет все районы
   - Поиск заработает по всем полям

3. **Пользователь сможет фильтровать:**
   - Выбрать "Западный обход" → увидит все объекты этого района
   - Выбрать "Центральный" → увидит объекты центра
   - Поиск работает по частичному совпадению (case-insensitive)

---

## 🎯 **Примеры парсинга адресов**

### Пример 1: Микрорайон
```
Адрес: "г. Краснодар, мкр. Западный обход, ул. Тургенева, 100"

Результат:
✅ parsed_district = "Западный обход"
✅ parsed_settlement = "Западный обход"
✅ parsed_area = "Западный"
🔗 district_id = 27 (если "Западный обход" есть в справочнике)
```

### Пример 2: Округ
```
Адрес: "Краснодар, Карасунский округ, ул. Красная, 1"

Результат:
✅ parsed_district = "Карасунский округ"
✅ parsed_area = "Карасунский"
🔗 district_id = 9 (если "Карасунский" есть в справочнике)
```

### Пример 3: Центральные улицы
```
Адрес: "г. Краснодар, ул. Красная, 32"

Результат:
✅ parsed_district = "Центральный" (определяется по известным улицам)
✅ parsed_area = "Центральный"
🔗 district_id = 10
```

---

## 📊 **Статистика после заполнения**

После запуска скрипта вы увидите:

```
============================================================
📊 Статистика по районам после обновления
============================================================

🏘️ Уникальных районов (parsed_district): 25
   • Западный обход
   • Карасунский
   • Центральный
   • Юбилейный
   • Гидростроителей
   ...

🏡 Уникальных микрорайонов (parsed_settlement): 15
   • Западный обход
   • ЖК "Новый Горизонт"
   • мкр. Юбилейный
   ...

📈 Покрытие:
   • Всего объектов: 110
   • С районами: 95
   • Процент: 86.4%
```

---

## 🚀 **Быстрый старт**

### Вариант 1: У вас уже есть объекты с адресами

```bash
# 1. Запустите автозаполнение
python scripts/auto_fill_districts_from_addresses.py

# 2. Проверьте результат
curl http://localhost:5000/api/districts/1 | python -m json.tool

# 3. Обновите страницу - районы появятся автоматически!
```

### Вариант 2: Вы импортируете новую базу

```bash
# 1. Импортируйте данные (используя ParserImportService или другой способ)
python scripts/your_import_script.py

# 2. Запустите автозаполнение районов
python scripts/auto_fill_districts_from_addresses.py

# 3. Готово! Районы и подсказки работают
```

---

## 🔧 **Расширенные возможности**

### Добавление новых районов в справочник

Если парсер найдёт район, которого нет в таблице `districts`:

```sql
INSERT INTO districts (name, city_id, slug)
VALUES ('Новый Микрорайон', 1, 'novy-mikrorayon');
```

Затем перезапустите скрипт - объекты автоматически свяжутся с новым районом.

### Интеграция с DaData (опционально)

Для более точного определения можно добавить DaData API:

```python
from services.dadata_client import DaDataClient

client = DaDataClient()
result = client.clean_address('г. Краснодар, ул. Красная, 1')

# DaData вернёт:
# - city_district = "Центральный округ"
# - settlement = None
# - area = "Центральный"
```

---

## ✅ **Результат**

После всех этапов:

1. ✅ **Все объекты имеют районы** в полях `parsed_district`, `parsed_settlement`, `parsed_area`
2. ✅ **API возвращает полный список районов** `/api/districts/1`
3. ✅ **Dropdown на главной странице** показывает все районы
4. ✅ **Фильтр работает** на странице `/properties`
5. ✅ **Поиск находит объекты** по названию района
6. ✅ **Подсказки обновляются автоматически** при каждом обновлении данных

---

## 🎯 **Часто задаваемые вопросы**

**Q: Нужно ли вручную добавлять районы?**  
A: Нет! Скрипт автоматически извлекает районы из адресов.

**Q: Что если адрес не содержит района?**  
A: Скрипт попробует определить район по известным улицам (например, ул. Красная → Центральный).

**Q: Как часто обновлять районы?**  
A: Запускайте скрипт после каждого импорта новых объектов.

**Q: Можно ли добавить свои правила парсинга?**  
A: Да! Отредактируйте функцию `parse_district_from_address()` в скрипте.

**Q: Что делать, если район определился неправильно?**  
A: Можно вручную обновить поле `parsed_district` в базе данных или добавить правило в скрипт.
