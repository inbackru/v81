# 🗺️ Полная система районов и микрорайонов - Руководство

## 📌 Обзор

Платформа InBack теперь имеет **полноценную систему районов** как в Яндекс.Недвижимости и Авито:

- ✅ **116 районов и микрорайонов** Краснодара в справочнике
- ✅ **Автоматическое определение района** при импорте объектов
- ✅ **Поиск и фильтрация** по районам
- ✅ **Подсказки в поисковой строке** с названиями районов
- ✅ **API для получения всех районов** города

---

## 🎯 Как это работает

### 1. **Справочник районов (District Table)**

Таблица `districts` содержит **полный список** всех районов, микрорайонов, округов города:

```sql
SELECT name FROM districts WHERE city_id = 1 ORDER BY name LIMIT 10;

-- Результат:
-- "40 лет Победы"
-- "Академгородок"
-- "Гидростроителей"
-- "Западный округ"
-- "Карасунский округ"
-- "мкр. Витаминкомбинат"
-- "мкр. Гидростроителей"
-- "Центр"
-- "Центральный округ"
-- ...
```

### 2. **API возвращает все районы**

```bash
GET /api/districts/1

{
  "success": true,
  "count": 116,
  "districts": [
    "40 лет Победы",
    "Академгородок",
    "Гидростроителей",
    ...
  ]
}
```

### 3. **Автоматическое заполнение при импорте**

Когда вы импортируете объекты с адресами:

```python
# Пример объекта
property = {
    'address': 'г. Краснодар, мкр. Западный обход, ул. Тургенева, 100',
    'rooms': 2,
    'price': 5500000
}

# После автозаполнения:
# - parsed_district = "Западный обход"
# - parsed_settlement = "Западный обход"
# - parsed_area = "Западный"
# - district_id = 27 (связь с таблицей districts)
```

### 4. **Поиск и подсказки**

При вводе в поисковую строку:

```
Пользователь вводит: "Западный"

Подсказки показывают:
┌─────────────────────────────┐
│ 🏘️ Район Западный          │
│ 🏘️ Район Западный обход    │
│ 🏢 ЖК Западный              │
│ 🏢 ЖК "На Западном"         │
└─────────────────────────────┘
```

---

## 🚀 Быстрый старт

### Шаг 1: Импорт справочника районов (уже выполнен)

```bash
# ✅ Уже выполнено - в БД 116 районов Краснодара
python scripts/import_krasnodar_districts.py
```

### Шаг 2: Импорт объектов с адресами

```python
# Ваш импорт (любой способ)
from services.parser_import_service import ParserImportService

properties = [
    {
        'external_id': 'prop_001',
        'address': 'г. Краснодар, мкр. Западный, ул. Красная, 32',
        'rooms': 2,
        'price': 5500000
    }
]

service = ParserImportService()
service.import_properties(properties)
```

### Шаг 3: Автоматическое заполнение районов

```bash
python scripts/auto_fill_districts_from_addresses.py
```

**Результат:**
- Объекты автоматически получают район
- Появляются в фильтрах
- Отображаются в подсказках поиска

---

## 📋 Полный список возможностей

### ✅ Для Пользователей

1. **Поиск по району:**
   - Введите "Западный" → увидите все объекты этого района
   - Автоматические подсказки с названиями районов

2. **Фильтрация на /properties:**
   - Dropdown "Район" показывает все районы города
   - Выберите район → объекты фильтруются автоматически

3. **Подсказки как в Яндекс.Недвижимости:**
   - "Район Западный"
   - "мкр. Бытха"
   - ЖК, застройщики, типы квартир

### ✅ Для Администраторов

1. **Автоматический импорт:**
   - Импортируйте объекты с адресами
   - Запустите скрипт автозаполнения
   - Районы определяются автоматически

2. **API для интеграций:**
   ```bash
   GET /api/districts/<city_id>
   ```

3. **Справочник постоянно расширяется:**
   - Добавляйте новые районы в БД
   - Автоматически появляются в UI

---

## 🛠️ Технические детали

### Структура данных

**Таблица `districts`:**
```sql
CREATE TABLE districts (
    id SERIAL PRIMARY KEY,
    city_id INTEGER REFERENCES cities(id),
    name VARCHAR(255) NOT NULL,
    slug VARCHAR(255),
    fias_id VARCHAR(36),
    kladr_id VARCHAR(20),
    created_at TIMESTAMP DEFAULT NOW()
);
```

**Связь с объектами:**
```sql
-- Таблица properties
ALTER TABLE properties ADD COLUMN district_id INTEGER REFERENCES districts(id);
ALTER TABLE properties ADD COLUMN parsed_district VARCHAR(255);
ALTER TABLE properties ADD COLUMN parsed_settlement VARCHAR(255);
ALTER TABLE properties ADD COLUMN parsed_area VARCHAR(255);
```

### Логика парсинга адресов

Скрипт `auto_fill_districts_from_addresses.py` использует regex паттерны:

```python
# Микрорайоны
"мкр. Западный" → parsed_settlement = "Западный"
"микрорайон Гидростроителей" → parsed_settlement = "Гидростроителей"

# Округа
"Карасунский округ" → parsed_district = "Карасунский округ"
"Западный район" → parsed_district = "Западный район"

# Известные улицы
"ул. Красная" → parsed_district = "Центральный" (определяется по таблице)
```

### API Endpoints

**1. Получить районы города:**
```http
GET /api/districts/<city_id>

Response:
{
    "success": true,
    "count": 116,
    "districts": ["40 лет Победы", "Академгородок", ...]
}
```

**2. Поисковые подсказки:**
```http
GET /api/search/suggestions?q=Западный&city_id=1

Response:
{
    "suggestions": [
        {
            "type": "district",
            "value": "Район Западный",
            "count": 45
        },
        ...
    ]
}
```

---

## 📊 Статистика покрытия

После запуска автозаполнения вы увидите:

```
============================================================
📊 Статистика по районам после обновления
============================================================

🏘️ Уникальных районов (parsed_district): 25
   • Западный обход
   • Карасунский
   • Центральный
   ...

🏡 Уникальных микрорайонов (parsed_settlement): 15
   • Западный обход
   • мкр. Юбилейный
   ...

📈 Покрытие:
   • Всего объектов: 110
   • С районами: 95
   • Процент: 86.4%
```

---

## 🔧 Расширение системы

### Добавление районов для других городов

**Вариант 1: Вручную (готовый справочник)**

Создайте файл `scripts/import_<city>_districts.py` по аналогии с Краснодаром:

```python
SOCHI_DISTRICTS = [
    "Лазаревский",
    "Центральный",
    "Хостинский",
    "Адлерский",
    "мкр. Бытха",
    "мкр. Мамайка",
    ...
]

# Импортируйте в БД
import_districts(city_id=2, districts=SOCHI_DISTRICTS)
```

**Вариант 2: Через DaData API**

```bash
# Получите API ключ на https://dadata.ru/api/
export DADATA_API_KEY="ваш_ключ"

# Запустите импорт
python scripts/import_districts_from_dadata.py --city-name "Сочи" --city-id 2
```

### Добавление новых районов вручную

```sql
INSERT INTO districts (city_id, name, slug)
VALUES 
    (1, 'Новый микрорайон', 'novy-mikrorayon'),
    (1, 'Парк Краснодар', 'park-krasnodar');
```

Районы автоматически появятся в:
- API `/api/districts/1`
- Dropdown фильтрах
- Поисковых подсказках

---

## ❓ Часто задаваемые вопросы

**Q: Как часто обновлять районы?**  
A: Запускайте `auto_fill_districts_from_addresses.py` после каждого импорта новых объектов.

**Q: Можно ли добавить свои правила парсинга?**  
A: Да! Отредактируйте функцию `parse_district_from_address()` в скрипте.

**Q: Что если район определился неправильно?**  
A: Обновите вручную в БД или добавьте правило в скрипт парсинга.

**Q: Как добавить интеграцию с DaData для автоматического определения?**  
A: Получите API ключ, добавьте в `.env`, используйте `import_districts_from_dadata.py`.

**Q: Работает ли система для других городов?**  
A: Да! Импортируйте районы для любого города с помощью скриптов.

---

## 🎉 Результат

После настройки системы:

1. ✅ **Все районы города** в справочнике
2. ✅ **API возвращает полный список** районов
3. ✅ **Dropdown фильтры** работают автоматически
4. ✅ **Поисковые подсказки** показывают районы
5. ✅ **Автоматическое определение** при импорте объектов
6. ✅ **Фильтрация работает** корректно по всем источникам

**Система готова к использованию!** 🚀
