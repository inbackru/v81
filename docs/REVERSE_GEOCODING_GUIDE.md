# 🗺️ Руководство по Reverse Geocoding (координаты → адрес)

## Обзор

В вашем проекте **уже реализовано** получение полного адреса с районом/микрорайоном по координатам. Используются два API:

1. **Yandex Geocoder API** - быстрый reverse geocoding
2. **DaData API** - детальная разбивка адреса с микрорайонами

---

## 🎯 Ваш случай: Найти квартиры в районе "Гидрострой"

### Шаг 1: Получить название района по координатам

```python
from services.geocoding import get_geocoding_service

# Координаты района Гидрострой
latitude = 45.0355
longitude = 38.9753

geocoding_service = get_geocoding_service()
result = geocoding_service.reverse_geocode(latitude, longitude)

if result:
    district_name = result['district']  # "Гидрострой"
    city_name = result['city']          # "Краснодар"
    full_address = result['formatted_address']
```

**Результат:**
```python
{
    'formatted_address': 'Россия, Краснодарский край, Краснодар, микрорайон Гидрострой',
    'city': 'Краснодар',
    'district': 'Гидрострой',
    'street': '',
    'house': '',
    'latitude': 45.0355,
    'longitude': 38.9753
}
```

### Шаг 2: Найти квартиры в этом районе

```python
from models import Property

properties = Property.query.filter(
    Property.parsed_district.ilike(f"%{district_name}%"),
    Property.is_active == True
).all()

print(f"Найдено квартир в районе '{district_name}': {len(properties)}")
```

---

## 🔍 Метод 1: Yandex Geocoder (быстрый)

**Файл:** `services/geocoding.py`

### Простой reverse geocoding

```python
from services.geocoding import get_geocoding_service

geocoding_service = get_geocoding_service()

# Координаты → адрес
result = geocoding_service.reverse_geocode(
    latitude=45.0355,
    longitude=38.9753
)

print(result['district'])  # Микрорайон/район
```

### Обогащение объекта недвижимости

```python
# Получить готовую структуру для Property модели
enriched = geocoding_service.enrich_property_address(
    latitude=45.0355,
    longitude=38.9753
)

# Результат:
{
    'parsed_city': 'Краснодар',
    'parsed_district': 'Гидрострой',
    'parsed_street': '',
    'full_address': 'Россия, Краснодарский край, Краснодар, микрорайон Гидрострой',
    'latitude': 45.0355,
    'longitude': 38.9753
}
```

**Преимущества:**
- ✅ Быстрый
- ✅ Встроенное кэширование (24 часа)
- ✅ Поддержка всех регионов России
- ✅ Бесплатный (до 25,000 запросов/день)

**Недостатки:**
- ⚠️ Менее детальная разбивка адреса
- ⚠️ Не различает административный район от микрорайона

---

## 🔎 Метод 2: DaData (детальный)

**Файл:** `services/dadata_client.py`

### Детальная разбивка адреса

```python
from services.dadata_client import get_dadata_client

dadata_client = get_dadata_client()

# Адрес → детальная разбивка
result = dadata_client.enrich_property_address(
    address_text="Краснодар, микрорайон Гидрострой, улица Российская",
    city_id=1  # ID города из базы
)

# Результат:
{
    'parsed_city': 'Краснодар',
    'parsed_area': 'Прикубанский',          # Административный район
    'parsed_settlement': 'Гидрострой',      # Микрорайон
    'parsed_street': 'Российская',
    'parsed_house': '',
    'parsed_block': '',
    'latitude': 45.0355,
    'longitude': 38.9753
}
```

**Преимущества:**
- ✅ Детальная разбивка: `area` (район) + `settlement` (микрорайон)
- ✅ FIAS ID для точной привязки к адресной базе
- ✅ Автоматическая нормализация адресов

**Недостатки:**
- ⚠️ Требует API ключ (платный)
- ⚠️ Работает с текстовыми адресами (не coordinates → address)

---

## 🚀 Автоматическое обогащение при импорте

**Файл:** `services/auto_geocoding.py`

При импорте новых квартир адреса обогащаются **автоматически**:

```python
from services.parser_import_service import ParserImportService

# При импорте квартиры
property_data = {
    'external_id': 'prop_001',
    'address': 'Краснодар, мкр Гидрострой, ул Российская, 10',
    'latitude': 45.0355,
    'longitude': 38.9753,
    'rooms': 2,
    'price': 5000000
}

# ParserImportService автоматически:
# 1. Создаёт квартиру в БД
# 2. Вызывает AutoGeocodingService
# 3. Обогащает через DaData
# 4. Заполняет parsed_city, parsed_area, parsed_settlement, parsed_street

result = ParserImportService.import_property(
    property_data,
    complex_name='ЖК Солнечный',
    city_name='Краснодар'
)

# Квартира автоматически получает:
# - parsed_city: "Краснодар"
# - parsed_area: "Прикубанский"
# - parsed_settlement: "Гидрострой"
# - parsed_street: "Российская"
```

---

## 📊 Сравнение методов

| Характеристика | Yandex Geocoder | DaData |
|---------------|-----------------|--------|
| **Входные данные** | Координаты (lat, lon) | Текстовый адрес |
| **Микрорайон** | `district` (общий) | `settlement` (отдельно) |
| **Административный район** | Не выделяет | `area` (отдельно) |
| **Скорость** | Быстрый | Средний |
| **Кэширование** | 24 часа | 1 час |
| **Стоимость** | Бесплатно (25k/день) | Платно |
| **Лучше для** | Координаты → адрес | Адрес → структура |

---

## 🧪 Тестирование

Запустите тестовый скрипт:

```bash
python scripts/test_reverse_geocoding.py
```

**Что делает скрипт:**
1. Тестирует Yandex Geocoder (координаты → адрес)
2. Тестирует DaData (адрес → детальная разбивка)
3. Демонстрирует поиск квартир в районе по координатам

---

## 🔑 Настройка API ключей

### Yandex Maps API

```bash
# .env
YANDEX_MAPS_API_KEY=ваш_ключ_яндекс
```

**Получить ключ:**
1. Зайдите на https://developer.tech.yandex.ru/
2. Создайте приложение
3. Выберите API: "Геокодер"
4. Скопируйте API ключ

### DaData API

```bash
# .env
DADATA_API_KEY=ваш_ключ_dadata
DADATA_SECRET_KEY=ваш_секретный_ключ  # опционально
```

**Получить ключ:**
1. Зайдите на https://dadata.ru/
2. Зарегистрируйтесь
3. Создайте токен в личном кабинете

---

## 💡 Рекомендации

### Для парсера с координатами:

**Используйте Yandex Geocoder:**
```python
from services.geocoding import get_geocoding_service

geocoding = get_geocoding_service()
result = geocoding.reverse_geocode(lat, lon)
district_name = result['district']
```

### Для парсера с текстовыми адресами:

**Используйте DaData:**
```python
from services.dadata_client import get_dadata_client

dadata = get_dadata_client()
result = dadata.enrich_property_address(address_text)
microdistrict = result['parsed_settlement']
```

### Для массового импорта:

**Используйте AutoGeocodingService + ParserImportService:**
```python
from services.parser_import_service import ParserImportService

# Импортируйте как обычно
# Обогащение произойдёт автоматически
ParserImportService.import_property(...)
```

---

## ❓ FAQ

**Q: Как найти квартиры в "Гидрострое", если знаю только координаты?**

```python
# 1. Координаты → название района
from services.geocoding import get_geocoding_service
geocoding = get_geocoding_service()
result = geocoding.reverse_geocode(45.0355, 38.9753)
district = result['district']  # "Гидрострой"

# 2. Поиск квартир в этом районе
from models import Property
properties = Property.query.filter(
    Property.parsed_district.ilike(f"%{district}%")
).all()
```

**Q: Какой API лучше для моего парсера?**

- Парсер возвращает **координаты** → Yandex Geocoder
- Парсер возвращает **текстовые адреса** → DaData
- Нужна **детальная разбивка** (район + микрорайон) → DaData

**Q: Можно ли использовать оба API одновременно?**

Да! Например:
1. Yandex Geocoder: координаты → базовый адрес
2. DaData: базовый адрес → детальная разбивка с микрорайоном

```python
# Шаг 1: Координаты → адрес (Yandex)
yandex_result = geocoding_service.reverse_geocode(lat, lon)
address_text = yandex_result['formatted_address']

# Шаг 2: Адрес → детальная разбивка (DaData)
dadata_result = dadata_client.enrich_property_address(address_text)
microdistrict = dadata_result['parsed_settlement']
```

---

## 📚 Дополнительные ресурсы

- **Yandex Geocoder API Docs:** https://yandex.com/dev/geocode/doc/en/
- **DaData API Docs:** https://dadata.ru/api/
- **Существующая документация парсера:** `scripts/PARSER_INTEGRATION_GUIDE.md`

---

**✅ Итог:** У вас уже всё готово для работы с районами и микрорайонами по координатам!
