# 📝 Руководство по импорту новых данных

## ✅ Автоматическая транслитерация slug

**С ноября 2025 года все новые застройщики, ЖК и квартиры автоматически получают SEO-friendly URL на латинице.**

---

## 🏗️ Импорт застройщиков

### Используйте:
```python
from utils.transliteration import create_developer_slug

# Автоматически создаст латинский slug
developer_slug = create_developer_slug("СПЕЦИАЛИЗИРОВАННЫЙ ЗАСТРОЙЩИК КОРОНА")
# Результат: "spetsializirovannyy-zastroyschik-korona"
```

### Примеры:
| Название застройщика | Slug (автоматически) |
|----------------------|----------------------|
| СЗ КАСКАД | sz-kaskad |
| КОРОНА | korona |
| Неометрия | neometriya |
| ТОЧНО | tochno |

---

## 🏘️ Импорт жилых комплексов (ЖК)

### Используйте:
```python
from utils.transliteration import create_complex_slug

# Автоматически удалит "ЖК" и создаст латинский slug
complex_slug = create_complex_slug("ЖК Лестория")
# Результат: "lestoriya"
```

### Примеры:
| Название ЖК | Slug (автоматически) |
|-------------|----------------------|
| ЖК Лестория | lestoriya |
| ЖК Южный парк | yuzhnyy-park |
| ЖК Гранд Каскад | grand-kaskad |

---

## 🏠 Импорт квартир

Квартиры используют `property_id` из внешних источников и не требуют создания slug.

---

## 📜 Обновленные скрипты

✅ **Скрипты с автоматической транслитерацией:**
- `scripts/populate_developers.py` - импорт застройщиков
- `app.py` - создание через веб-интерфейс
- `utils/transliteration.py` - единая функция транслитерации

⚠️ **Старые скрипты (требуют обновления при использовании):**
- `scripts/create_hierarchy_from_excel.py`
- Другие кастомные скрипты импорта

---

## 🔧 Как использовать в новых скриптах

```python
#!/usr/bin/env python3
import sys
import os

# Добавить корневую директорию в path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

# Импортировать функции транслитерации
from utils.transliteration import create_developer_slug, create_complex_slug

# Создать застройщика с правильным slug
from models import Developer
from app import db

developer = Developer(
    name="Новый Застройщик 2025",
    slug=create_developer_slug("Новый Застройщик 2025"),  # → "novyy-zastroyschik-2025"
    description="Описание компании"
)
db.session.add(developer)
db.session.commit()
```

---

## ✨ Преимущества

1. **SEO-friendly URL**: `/developer/sz-kaskad` вместо `/developer/%D1%81%D0%B7-%D0%BA%D0%B0%D1%81%D0%BA%D0%B0%D0%B4`
2. **Читаемые ссылки**: Пользователи и поисковики понимают, о чём страница
3. **Совместимость**: Старые кириллические URL продолжают работать (fallback)
4. **Единообразие**: Все данные используют одну систему транслитерации

---

## 📞 Поддержка

При возникновении проблем с импортом данных:
1. Проверьте, используете ли вы функции из `utils.transliteration`
2. Убедитесь, что slug создаётся автоматически, а не вручную
3. Протестируйте импорт на небольшой выборке данных перед полным импортом
