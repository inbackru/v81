# 🤖 Руководство по интеграции парсера

## ✅ Автоматический импорт данных с SEO-friendly URL

Система **ParserImportService** автоматически:
- ✅ Создаёт **читаемые латинские slug** (транслитерация)
- ✅ Устанавливает **связи между сущностями** (FK)
- ✅ **Обновляет существующие записи** (не дублирует)
- ✅ Создаёт **иерархию**: Застройщик → ЖК → Корпус → Квартира

---

## 📋 Иерархия данных

```
Застройщик (Developer)
    ↓ developer_id
ЖК (ResidentialComplex)
    ↓ complex_id
Корпус (Building)
    ↓ building_id
Квартира (Property)
```

---

## 🚀 Быстрый старт

### 1️⃣ Импорт полной иерархии

```python
from services.parser_import_service import ParserImportService

# Данные от парсера
developer_data = {
    'name': 'НОВЫЙ ЗАСТРОЙЩИК 2025',
    'external_id': 'dev_2025_001',  # ID из вашего источника
    'website': 'example.com',
    'phone': '+7 (XXX) XXX-XX-XX'
}

complex_data = {
    'name': 'ЖК Новый Горизонт',
    'external_id': 'complex_2025_001',
    'address': 'г. Краснодар, ул. ...',
    'latitude': 45.040000,
    'longitude': 38.980000
}

buildings_data = [
    {
        'name': 'Корпус 1',
        'external_id': 'building_001',
        'end_build_year': 2026,
        'end_build_quarter': 2
    }
]

properties_data = [
    {
        'external_id': 'prop_001',  # ОБЯЗАТЕЛЬНО
        'building_name': 'Корпус 1',
        'rooms': 2,
        'area': 65.5,
        'floor': 5,
        'price': 5500000
    }
]

# Импорт всей иерархии одной командой
result = ParserImportService.import_full_hierarchy(
    developer_data=developer_data,
    complex_data=complex_data,
    buildings_data=buildings_data,
    properties_data=properties_data,
    city_name='Краснодар'
)

# result содержит:
# {
#     'developer': Developer,
#     'complex': ResidentialComplex,
#     'buildings': [Building, ...],
#     'properties': [Property, ...]
# }
```

---

## 📊 Поля для импорта

### Застройщик (Developer)

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `name` | str | ✅ Да | Название застройщика |
| `external_id` | str | ⚠️ Рекомендуется | ID из внешнего источника |
| `full_name` | str | Нет | ООО "Название" |
| `website` | str | Нет | example.com |
| `phone` | str | Нет | +7 (XXX) XXX-XX-XX |
| `email` | str | Нет | info@example.com |
| `address` | str | Нет | г. Краснодар, ул. ... |
| `latitude` | float | Нет | 45.035470 |
| `longitude` | float | Нет | 38.975313 |
| `established_year` | int | Нет | 2010 |
| `description` | str | Нет | Описание компании |

**Автоматически создаётся:**
- `slug` → `novyy-zastroyschik-2025` (латиница)

---

### ЖК (ResidentialComplex)

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `name` | str | ✅ Да | Название ЖК |
| `external_id` | str | ⚠️ Рекомендуется | ID из внешнего источника |
| `address` | str | Нет | ул. ... |
| `phone` | str | Нет | +7 (XXX) XXX-XX-XX |
| `latitude` | float | Нет | 45.040000 |
| `longitude` | float | Нет | 38.980000 |
| `description` | str | Нет | Описание ЖК |
| `complex_type` | str | Нет | `residential` или `cottage` |
| `class_name` | str | Нет | Комфорт / Бизнес / Премиум |

**Автоматически создаётся:**
- `slug` → `novyy-gorizont` (латиница, без "ЖК")
- `developer_id` → автоматическая связь
- `city_id` → автоматическая связь

---

### Корпус (Building)

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `name` | str | ✅ Да | Корпус 1 / Литер А |
| `external_id` | str | ⚠️ Рекомендуется | ID из внешнего источника |
| `end_build_year` | int | Нет | 2026 |
| `end_build_quarter` | int | Нет | 1, 2, 3, 4 |
| `released` | bool | Нет | Сдан в эксплуатацию |
| `has_accreditation` | bool | Нет | Аккредитация банков |
| `has_green_mortgage` | bool | Нет | Зелёная ипотека |

**Автоматически создаётся:**
- `slug` → `korpus-1` (латиница)
- `complex_id` → автоматическая связь

---

### Квартира (Property)

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `external_id` | str | ✅ **ДА** | Уникальный ID из источника |
| `building_name` | str | Нет | Название корпуса |
| `rooms` | int | Нет | Количество комнат (0 = студия) |
| `area` | float | Нет | Площадь в м² |
| `floor` | int | Нет | Этаж |
| `total_floors` | int | Нет | Всего этажей в доме |
| `price` | int | Нет | Цена в рублях |
| `price_per_sqm` | int | Нет | Цена за м² |
| `has_balcony` | bool | Нет | Наличие балкона |
| `has_loggia` | bool | Нет | Наличие лоджии |
| `ceiling_height` | float | Нет | Высота потолков (м) |
| `finishing` | str | Нет | Без отделки / Чистовая |

**Автоматически создаётся:**
- `slug` → `novyy-gorizont-prop_001` (латиница)
- `title` → "2-комн. квартира, 65.5 м²"
- `complex_id` → автоматическая связь
- `developer_id` → автоматическая связь
- `building_id` → автоматическая связь (если указан building_name)
- `city_id` → автоматическая связь

---

## 🔄 Обновление существующих данных

### Логика обновления:

1. **Поиск существующей записи:**
   - По `external_id` (если указан)
   - По `name` (если external_id не найден)

2. **Действия:**
   - Если найдена → **обновляет поля**
   - Если не найдена → **создаёт новую**

### Пример обновления цены квартиры:

```python
# Парсер нашёл обновлённую цену
updated_property = {
    'external_id': 'prop_001',  # Существующая квартира
    'price': 5400000,  # Новая цена
    'price_per_sqm': 82400
}

property_obj = ParserImportService.import_property(
    updated_property,
    complex_name='ЖК Новый Горизонт',
    building_name='Корпус 1',
    city_name='Краснодар'
)
# Цена обновлена автоматически!
```

---

## 🏙️ Мультигород

По умолчанию: `city_name='Краснодар'`

Поддерживаемые города:
- Краснодар
- Сочи
- Анапа
- Геленджик
- Новороссийск
- Армавир
- Туапсе
- Майкоп

```python
# Импорт для Сочи
result = ParserImportService.import_full_hierarchy(
    developer_data=...,
    complex_data=...,
    buildings_data=...,
    properties_data=...,
    city_name='Сочи'  # ✅
)
```

---

## ⚡ Массовый импорт (10,000+ квартир)

```python
from app import app
from services.parser_import_service import ParserImportService

with app.app_context():
    # 1. Создаём застройщика и ЖК один раз
    developer = ParserImportService.import_developer(developer_data)
    complex = ParserImportService.import_residential_complex(
        complex_data, 
        developer_name=developer.name,
        city_name='Краснодар'
    )
    
    # 2. Импортируем корпуса
    for building_data in buildings_list:
        ParserImportService.import_building(
            building_data,
            complex_name=complex.name,
            city_name='Краснодар'
        )
    
    # 3. Импортируем квартиры пачками (batch)
    batch_size = 1000
    for i in range(0, len(properties_list), batch_size):
        batch = properties_list[i:i+batch_size]
        
        for property_data in batch:
            ParserImportService.import_property(
                property_data,
                complex_name=complex.name,
                building_name=property_data.get('building_name'),
                city_name='Краснодар'
            )
        
        print(f"Импортировано {i+len(batch)} квартир")
```

---

## 🔍 Автоопределение проданных квартир

Система автоматически отслеживает `last_seen_at` для каждой квартиры.

**Как работает:**
1. Парсер импортирует квартиры → `last_seen_at` обновляется
2. Квартира исчезла из источника → `last_seen_at` не обновляется
3. Система автоматически помечает её как проданную
4. Пользователи получают уведомления

**Подробнее:** См. раздел "Automatic Sold Property Detection System" в `replit.md`

---

## 🧪 Тестирование

Запустите пример:

```bash
python scripts/example_parser_import.py
```

**Результат:**
```
✅ ИМПОРТ ЗАВЕРШЁН УСПЕШНО!

📍 Застройщик: НОВЫЙ ЗАСТРОЙЩИК 2025
   Slug: novyy-zastroyschik-2025
   URL: /developer/novyy-zastroyschik-2025

🏘️  ЖК: ЖК Новый Горизонт
   Slug: novyy-gorizont
   URL: /krasnodar/zhk/novyy-gorizont

🏢 Корпусов: 2
   - Корпус 1 (slug: korpus-1)
   - Корпус 2 (slug: korpus-2)

🏠 Квартир: 3
   - 2-комн. квартира, 65.5 м² (external_id: prop_2025_001)
   - 1-комн. квартира, 42.3 м² (external_id: prop_2025_002)
   - 3-комн. квартира, 85.2 м² (external_id: prop_2025_003)
```

---

## ❓ FAQ

### В: Что если парсер добавит застройщика с таким же именем?
**О:** Система найдёт существующего и обновит его данные, не создаст дубликат.

### В: Что если не указать external_id?
**О:** Для застройщиков и ЖК — можно. Для квартир — **обязательно** (нужен для обновлений и системы "продано").

### В: Можно ли импортировать только квартиры без застройщика?
**О:** Да, но ЖК должен уже существовать в БД. Используйте `import_property()` напрямую.

### В: Что если парсер нашёл новое поле в БД?
**О:** Добавьте поле в модель → обновите `ParserImportService` → парсер автоматически начнёт его заполнять.

### В: Slug генерируется каждый раз при импорте?
**О:** Нет, slug создаётся один раз при первом импорте. При обновлениях slug не меняется.

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `logger.info/error` в консоли
2. Убедитесь, что город существует в БД (таблица `cities`)
3. Проверьте, что все обязательные поля заполнены
4. Используйте `external_id` для надёжного обновления данных
